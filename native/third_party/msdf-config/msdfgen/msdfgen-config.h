#pragma once
// Minimal hand-written msdfgen config for the text engine (no Skia geometry
// preprocessing, no extensions). Defines MSDFGEN_PUBLIC so base.h skips the
// generated-config include path.
#define MSDFGEN_VERSION 1.12.1
#define MSDFGEN_PUBLIC
